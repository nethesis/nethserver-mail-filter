# /etc/rspamd/local.d/redis.conf
servers = "/var/run/redis/rspamd";

# Servers to write data
write_servers = "/var/run/redis/rspamd";

# List of modules that should not use redis from this section
#disabled_modules = ["ratelimit"];

timeout = 1s;
