#
# 30postgrey
# {
    if($postgrey{status} eq 'enabled') {
	@smtpd_recipient_restrictions = map {
	    $_ eq 'reject_unauth_destination' ?
		($_, 'check_policy_service unix:postgrey/socket') :
		$_
	} @smtpd_recipient_restrictions; 
    }
    '';
}

